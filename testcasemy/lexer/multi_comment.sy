/* 多行注释示例
   第二行
   第三行
*/

// 单行注释示例

int main() {
    /* 初始化变量 */
    int a = 10;       // a 的初值
    float b = 3.14;   /* 浮点数 b 的初始化 */

    a = a + b;  // 表达式末尾的单行注释
    b = b * 2.0; /* 浮点乘法 */ 

    /* 在表达式内部进行注释 */
    int c = 5 /* 临时变量 */ + 7; 

    float d = 1.0
        /* 跨行注释 */
        + 2.0;

    // 混合注释和操作符
    a = a + 1 /* 加一 */ + 2 /* 加二 */;

    return 0; /* 返回值注释 */
}